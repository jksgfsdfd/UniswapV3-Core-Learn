describe("Factory", function () {
  it("Doesn't allow delegate call on the createPool method", async function () {});

  it("Results in lower gas usage when initializing contracts using callback", async function () {});

  it("Results in lower gas usage when the pair is accessible both ways than comparing them before accessing", async function () {});
});
